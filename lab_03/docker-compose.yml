services:
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.retention=12h'
      - '--storage.tsdb.wal-compression'

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - ./grafana/data:/var/lib/grafana
    depends_on:
      - prometheus

  gatling-at-once:
    image: denvazh/gatling:latest
    container_name: gatling-at-once
    volumes:
      - ./gatling/simulations:/opt/gatling/user-files/simulations
      - ./gatling/results:/opt/gatling/results
    depends_on:
      - echo-ping
      - fasthttp-ping
    entrypoint: ["/bin/sh", "-c", "gatling.sh -s ServerAtOnceLoadSimulation"]

  gatling-per-second:
    image: denvazh/gatling:latest
    container_name: gatling-per-second
    volumes:
      - ./gatling/simulations:/opt/gatling/user-files/simulations
      - ./gatling/results:/opt/gatling/results
    depends_on:
      gatling-at-once:
        condition: service_completed_successfully
    entrypoint: ["/bin/sh", "-c", "gatling.sh -s ServerPerSecondLoadSimulation"]

  echo-ping:
    image: go_env:latest
    deploy:
      resources:
        limits:
          cpus: '1'  # Максимум 50% доступных процессоров
          memory: '400M'  # Максимум 256 МБ памяти
    container_name: echo-ping
    ports:
      - "8081:8081"
    depends_on:
      - prometheus
    entrypoint: ["/bin/sh", "-c", "go run prometheus/echo/core.go"]

  fasthttp-ping:
    image: go_env:latest
    deploy:
      resources:
        limits:
          cpus: '1'  # Максимум 50% доступных процессоров
          memory: '400M'  # Максимум 256 МБ памяти
    container_name: fasthttp-ping
    ports:
      - "8082:8081"
    depends_on:
      - prometheus
    entrypoint: ["/bin/sh", "-c", "go run prometheus/fasthttp/core.go"]

  chi-ping:
    image: go_env:latest
    deploy:
      resources:
        limits:
          cpus: '1'  # Максимум 50% доступных процессоров
          memory: '400M'  # Максимум 256 МБ памяти
    container_name: chi-ping
    ports:
      - "8083:8081"
    depends_on:
      - prometheus
    entrypoint: [ "/bin/sh", "-c", "go run prometheus/chi/core.go" ]